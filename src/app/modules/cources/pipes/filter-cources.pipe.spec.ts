import { FilterCourcesPipe } from './filter-cources.pipe';

describe('FilterCourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
